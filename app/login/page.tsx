import AuthForm from '@/components/AuthForm';

export const metadata = {
  title: 'Login | Ölüdeniz Paragliding Tours',
  description: 'Sign in to your account or create a new one',
};

export default function LoginPage() {
  return <AuthForm />;
}
